name: Core tests

on:
  push:
    branches: [main]
    paths:
      - "Core/**"
      - ".github/workflows/core-tests.yml"
  pull_request:
    paths:
      - "Core/**"
      - ".github/workflows/core-tests.yml"

jobs:
  test:
    runs-on: macos-26

    steps:
      - uses: actions/checkout@v4

      # HydeTests.testFetch hits the live hyde.dk network, so CI runs only
      # ParserTests and FetcherTests, which use fixtures/no network.
      - name: Run ParserTests and FetcherTests
        working-directory: Core
        run: swift test --filter 'ParserTests|FetcherTests'
